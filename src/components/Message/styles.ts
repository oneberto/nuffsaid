import { Button } from "@mui/material";
import styled from "styled-components";

export const MessageClearButton = styled(Button)`
  margin-left: auto;
  text-transform: none;
  color: #35301d;
  padding: 0;
  font-size: 11px;
  font-weight: normal;
  min-width: unset;
`;
